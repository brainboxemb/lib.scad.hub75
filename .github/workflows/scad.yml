name: SCAD production

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: write
  packages: read

jobs:
  scad:
    uses: brainboxemb/tool.scad-project/.github/workflows/project-production.yml@28661fc040c4994e9c1d391285b7425c7a55252b
    with:
      affected_task: consumer:scad.production-impact
      aggregate_task: consumer:scad.ci
      cache_namespace: lib-scad-hub75-production-v1
      build_artifact_name: lib-scad-hub75-build-publication
      verification_artifact_name: lib-scad-hub75-verification-publication
